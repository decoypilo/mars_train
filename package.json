{
  "name": "parcel",
  "version": "0.0.1",
  "author": "marzipan <decoy@dhanyapilo.com>",
  "license": "Mars College",
  "scripts": {
    "clean": "rimraf www/",
    "start": "yarn run clean && parcel serve src/index.html --out-dir www -p 8080",
    "build": "yarn run clean && parcel build src/index.html --out-dir www"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
